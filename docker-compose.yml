services:
  llm-learning:
    build: .
    container_name: llm-engineering-learning
    restart: unless-stopped
    ports:
      - "8765:8765"
    environment:
      HOST: 0.0.0.0
      PORT: 8765
      LLM_LEARNING_DATA_DIR: /data
    volumes:
      - ./data:/data
