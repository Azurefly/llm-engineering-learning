__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# All files under data/ are private local learning state: SQLite, WAL files,
# restore snapshots, notes/resources embedded in backups, etc. Keep only the
# empty directory marker in Git.
data/*
!data/.gitkeep
